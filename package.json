{
  "name": "aw-components",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "precommit-msg": "echo 'Build library files in Pre-commit...' && exit 0"
  },
  "devDependencies": {
    "pre-commit": "^1.2.2",
    "vite": "^2.9.9"
  },
  "pre-commit": [ "build" ]
}
